name: Deploy Next.js to GitHub Pages

on:
  push:
    branches: [ main ]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Install dependencies
        run: npm ci

      - name: Build with Next.js
        run: npm run build
        env:
          NODE_ENV: production
          GITHUB_ACTIONS: true
          NEXT_PUBLIC_BASE_PATH: /nextjs-test-blog
        
      - name: Make script executable
        run: chmod +x scripts/fix-paths.sh
        
      - name: Fix paths for GitHub Pages
        run: ./scripts/fix-paths.sh
        
      - name: Check paths after fixing (debug)
        run: |
          echo "Checking paths in HTML files after fixing:"
          grep -n "_next" out/index.html | head -5 || echo "No _next paths found"
          grep -n "nextjs-test-blog" out/index.html | head -5 || echo "No nextjs-test-blog paths found"
        
      - name: List build output (debug)
        run: ls -la ./out

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./out

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
